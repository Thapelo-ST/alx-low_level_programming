#include "main.h"

/**
 * add - adds 2 numbers and save answer
 * @a: first number
 * @b: second number
 * @sum: stores sum
 * Return: Sum
 */

int add(int a, int b)
{
	int sum = a + b;
	return (sum);
}
